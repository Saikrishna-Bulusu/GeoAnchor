# Runtime. Every one of these has an aarch64 wheel.
numpy
opencv-python-headless>=4.8,<5   # OpenCV 5 moved the 2D-features constructors and
                                 # cv2.AKAZE_create disappears. Pinned deliberately.
PyYAML
pyzmq                            # the layer bus
pyproj                           # pixel -> geodetic. rasterio is NOT needed at runtime.
pymavlink                        # actual GPS in, predicted position out
fastapi
uvicorn[standard]
websockets                       # uvicorn answers a WS upgrade with 403 without it
python-multipart                 # FastAPI needs it for UploadFile; without it a
                                 # map upload fails with a RuntimeError at request time,
                                 # not at import, so the dashboard looks broken rather
                                 # than the dependency missing.
pillow
tqdm                             # NOT ours: accelerated_features/modules/xfeat.py imports it
                                 # at module scope. methods.XFeatMethod.available() checks only
                                 # torch and the weights file, so preflight reports xfeat as
                                 # available and the first detect() is where it actually dies.

# torch and kornia are installed by bootstrap.sh, not from here. On aarch64 the
# plain `pip install torch` resolves to a CUDA build from PyPI, which is wrong
# on every board this project targets, so the wheel has to be chosen by hand.
